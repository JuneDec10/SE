泡利矩阵

在数学和数学物理中，包立矩阵是一组三个2×2的么正厄米复矩阵，一般都以希腊字母σ来表示，但有时当他们在和同位旋的对称性做连结时，会被写成τ。他们在包立表像（σ表像）可以写成：

这些矩阵是以物理学家沃尔夫冈·包立命名的。在量子力学中，它们出现在包立方程式中描述磁场和自旋之间交互作用的一项。所有的包立矩阵都是厄米矩阵，它们和单位矩阵（有时候又被称为为第零号包立矩阵），的线性张成为2×2厄米矩阵的向量空间。

从量子力学的角度来看，埃尔米特矩阵（算符）代表可观测的物理量，因此，σ, "k"= 0,1,2,3的线性张成代表所有作用在二维希尔伯特空间的物理量所形成的空间。从包立本人的的研究来看， , "k"=1,2,3所代表的物理量是自旋在三维欧几里得空间中第"k"个座标轴的投影分量。

三个包立矩阵可以共同用一种单一形式表达：

其中是克罗内克"δ"函数。当"a"="b"时，其值为1；当"a"≠"b"时，其值为0。

这些矩阵是对合的：

其中"I"是单位矩阵。

此外，包立矩阵的行列式和它们的迹分别为：

故从上述关系可以推得每个包立矩阵"σ"的本征值分别为±1。

每个包立矩阵有两个本征值，+1和−1，其对应的归一化本征向量为：

包立向量定义为：

这个定义提供了将一般向量基底对应到包立矩阵的基底的机制
相同的下标是使用了爱因斯坦求和约定。此外：

包立矩阵有以下的对易关系：

以及以下的反对易关系。

其中"ε"是列维-奇维塔符号，"δ"是克罗内克函数，是"I"是2 ×2的单位矩阵。而一样的，上面使用了爱因斯坦求和约定。

将包立矩阵的对易和反对易相加得：

因此可得：

为了避免符号重复，将"a", "b", "c"改成"p", "q", "r"，然后把上式和三维向量"a"和"b"内积，可得：

将它转换成向量积的表达式：

令formula_15，而且formula_16对于偶数"n"可得：

另外加上之前求得在"n" = 1的情况可在"n"为奇数的情况：

利用矩阵指数的概念，加上正弦和余弦的泰勒级数展开式，可得：

第一项的总和为formula_20，第二项括号里的总和是formula_21，于是：

这可以看做是欧拉公式的类比。

另一个常用来区别包立矩阵的方法是用上标，用不同的来代表不同的包立矩阵，而下标则代表不同的矩阵元素。因此第个包立矩阵的第行第列的元素可表示为

利用这种表示方法，包立矩阵的完备性关系可写作：

有时习惯上将2×2单位举写成"σ"，也就是，"σ" = "δ"。如此一来完备性关系可以更为简洁的表示成： 

令算符为换位算符（或称为置换算符）。对于两个在张量积空间中的自旋和该算符有：

的关系。这个算符可以更进一步的用包立矩阵来表示：

该算符有两个本征值，分别1和-1，这个算符可以用于代表某些哈密顿量的交互作用项，产生对称和反对称的本征态分裂的效果。

的实数张成与四元数的实代数同构，可透过下列映射得到对应关系（注意到包立矩阵的负号）：

另外一种方式的映射为将包立矩阵的次序反转

既然单位四元数与为群同构，此亦代表包立矩阵也可用来描述。从到的2对1同态性，也可以用包立矩阵来表述。

四元数构成可除代数——所有非零元素皆有反元素，然而包立矩阵并非如此。包立矩阵生成的代数的四元数版，参见复四元数，其共有8个实维度。


