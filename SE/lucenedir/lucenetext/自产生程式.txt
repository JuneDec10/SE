自产生程式

自产生程式（Quine），它以哲学家奎恩命名，指的是输出结果为程式自身源码的程式。

能够直接读取自己源码、读入使用者输入或空白的程式一般都不视为自产生程式。

这种编程思想在计算机刚刚兴起的时候就已经出现了。Paul Bratley发表的文章"Computer Recreations: Self-Reproducing Automata"也对此进行了讨论。而已知最早的这类程式在1960年代于爱丁堡大学出现，由Hamish Dewar以编写：

我们先定义一个函数formula_1，对于一个字串formula_2，formula_3经过程式语言的解释会变成formula_4。

对于一个程式formula_5而言，以下会使用formula_6来表示程式P的描述（即程式码）。

建立一个程式SELF，SELF由A、B所组成。换言之formula_7。且会先执行A再执行B。

而自生实际执行的过程为：

在Quine的定义里，程序不能有任何形式的输入，否则将被视为“作弊”。例如，一个程序读取该程序自身的源代码然后打印出来，利用这种方法的程序属于作弊的Quine。

一个用Perl编写的无作弊的Quine：

Python本身提供repr()或运算``，其作用大致等同于上述之q()。如：

把以下的话抄一遍后再抄在引号里「把以下的话抄一遍后再抄在引号里」


