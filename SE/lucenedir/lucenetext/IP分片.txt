IP分片

互联网协议（IP）允许资料包（）进行分片（，又译分段），将资料包分割成更小的单位。这样的话，当数据包比链路最大传输单元（MTU）大时，就可以被分解为很多的足够小片段，以便能够在其上进行传输。

RFC 1191 描述了“路径MTU发现”，该技术用于确定两台IP主机间的路径MTU，这样就可以避免IP分片。

在Internet协议IPv4版本和较新的IPv6版本中，分片机制的细节和分片机制的整体框架是有所不同的。
